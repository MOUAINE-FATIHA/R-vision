🟦 Jour 2 : SQL - DDL & DML

1.Quelle est la différence entre DDL (CREATE, ALTER) et DML (INSERT, UPDATE,DELETE)?
il permet de créer ou modifier la structure de la base de données, par exemple créer une table ou la modifier.
par contre DML,permet de manipuler les données à l’intérieur des tables, comme ajouter, modifier ou supprimer des lignes.


2. À quoi sert une clé primaire ?
Une clé primaire sert à identifier chaque enregistrement de façon unique dans une table.

3. Comment filtre-t-on des données avec la clause WHERE ?
On utilise la clause WHERE pour sélectionner seulement les lignes qui respectent une condition précise

● Challenges Pratiques :
1. Challenge 1 : Écrire la requête CREATE TABLE pour une table Produits (id, nom,
prix, stock).

Challenges Pratiques :
1. Challenge 1 : Écrire la requête CREATE TABLE pour une table Produits (id, nom,
prix, stock).

create table Produits (
    id int primary key AUTO_INCREMENT,
    nom varchar(50),
    prix decimal(10,2),
    stock int
);

2. Challenge 2 : Écrire les requêtes INSERT pour ajouter 3 nouveaux produits dans
la table.

insert into Produits (nom, prix, stock) 
values('Clavier', 250, 10),
('Souris', 120, 0),
('Écran', 1800, 5);

3. Challenge 3 : Écrire une requête UPDATE pour augmenter le prix d'un produit
spécifique de 10%, puis une requête DELETE pour supprimer un produit en
rupture de stock.

-->augmentation du prix:
update Produits
set prix = prix * 1.10
where id = 1;

--> Suppression:
delete from Produits
where stock =0;

🟦 Jour 3 : SQL - Agrégats Simples :

Questions Théoriques :
1.​ À quoi servent les fonctions COUNT, SUM, et AVG ?

count :permet de compter le nombre d'enregistrement
sum: permet de calculer le total.
avg  :permet de calculer une moyenne.

2.​ Peut-on utiliser COUNT(*) et COUNT(colonne) de manière interchangeable ?
Pourquoi ?

Non , count(*) compte toutes les lignes,par contre count(colonne) ne compte que les lignes où la colonne n'est pas nulle.

3.​ Comment MAX et MIN peuvent-ils être utilisés sur des dates ou des chaînes de caractères ?

concernant les dates , max donne la date la plus récente et min la plus ancienne.
concernent les chaînes de caractères , ils fonctionnent selon l'ordre alphabétique.

Challenges Pratiques :
1.​ Challenge 1 : Écrire la requête pour afficher le nombre total de commandes.
select count(*) from commandes ;
2.​ Challenge 2 : Écrire la requête pour calculer le prix moyen de tous les produits.
select avg(prix) from produits ;
3.​ Challenge 3 : Écrire la requête pour trouver le montant de la commande la plus chère
select max(montant) from commandes;


🟦 Jour 7 : SQL - Jointures
●​ Questions Théoriques :
1.​ Quelle est la différence entre INNER JOIN et LEFT JOIN ?

inner join affiche seulement les lignes qui ont une correspondance dans les deux tables , par contre left join affiche toutes les lignes de la table gauche meme si elles n'ont pas de correspondance dans l'autre table.


2.​ Dans quel cas utiliser un RIGHT JOIN ?

si on veut garder toutes les lignes de la table droite meme s'il n'y a pas de correspondance dans la table gauche.

3.​ Peut-on joindre plus de deux tables dans une seule requête ?
oui on peut faire plusieurs jointure si il existe les clés etrangeres.

●​ Challenges Pratiques :
1.​ Challenge 1 : (INNER JOIN) Afficher les commandes avec le nom du client
correspondant (tables Commandes et Clients).

select c.article , cl.nom from commandes c
inner join client cl on c.id_client = cl.id ;

2.​ Challenge 2 : (LEFT JOIN) Afficher tous les clients, et le montant de leur
commande s'ils en ont une (afficher NULL sinon).

select cl.nom , c.montant from clients cl 
left join commandes c on c.id_client = cl.id ;

Challenge 3 : (JOIN 3 tables) Afficher le détail des commandes : nom du client,
nom du produit et quantité commandée (tables Clients, Commandes,
Produits, Lignes_Commandes).

select cl.nom , p.nom , l.quantite from clients cl 
inner join commandes on c.id_client = cl.id 
inner join Lignes_Commandes l on c.id= l.id_commande
inner join produits on l.id_produit = p.id;

🟦 Jour 8 : SQL - Groupement et Tri

●​ Questions Théoriques :
1.​ Quelle est la différence entre WHERE et HAVING ?
where filtre les lignes avant l'aggrégation par contre having est utiliser avec group by , il filtre les resultats agrégées

2.​ Dans quel ordre logique SQL exécute-t-il FROM, WHERE, GROUP BY, HAVING,
SELECT, ORDER BY ?
from , where , group by , having , select , order by

3.​ Peut-on utiliser un alias de colonne (AS nom_alias) dans une clause WHERE ?
Et dans ORDER BY ?
pour where non car where  s'éxecute avant select et pour order by oui c'est possible.

●​ Challenges Pratiques :
1.​ Challenge 1 : (GROUP BY) Afficher le chiffre d’affaires total par client.
select id_client , sum(montant) as chiffreAffaire from commandes 
group by id_client;

2.​ Challenge 2 : (ORDER BY) Afficher les produits classés par prix, du plus cher au
moins cher.
select id_produit , prix from produit 
order by prix DESC;

3.​ Challenge 3 : (HAVING) Afficher uniquement les clients ayant passé plus de 2
commandes.
select id_client , count(*) as nbCommandes from commandes
group by id_client 
having count(*) > 2;
